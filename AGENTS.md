# 项目根目录
- 定位：统一管理火灾疏散仿真平台的前后端代码、配置与文档。
- 包含内容：
  - 后端源码位于 `backend/`，负责 FastAPI 仿真服务。
  - 前端源码位于 `frontend/`，提供 Vue3 + Three.js 的三维可视化界面。
  - 项目规范、环境说明等维护在 `docs/` 与根级配置文件中。
- 关联关系：
  - 与所有子目录存在依赖关系，是协作入口。
