# Ubuntu 执行教程

## 手动运行

1. 准备环境  
   - 确保系统具备 `python3.11+`、`uv`、`bun` 以及 `nodejs 18+`。若缺失，可通过 `sudo apt-get install python3.11 python3.11-venv` 与 `curl -fsSL https://bun.sh/install | bash` 等方式安装。  
   - 首次运行前，在项目根目录执行 `uv sync` 同步后端依赖，并在 `frontend/` 目录执行 `bun install`。

2. 启动后端  
   - 在项目根目录执行：  
     ```bash
     uv run uvicorn backend.app.main:app --reload
     ```  
   - 若需指定日志输出，可在命令末尾追加 `> logs/backend.log 2>&1`。

3. 启动前端  
   - 另开一个终端，仍在项目根目录执行：  
     ```bash
     (cd frontend && bun dev --host)
     ```  
   - 默认会在 `http://localhost:5173` 启动开发服务器。

4. 访问页面  
   - 浏览器打开 `http://localhost:5173`，即可连接正在运行的后端与前端。

5. 停止服务  
   - 在前端、后端终端分别使用 `Ctrl+C` 停止。

## 脚本运行

1. 确认已在项目根目录，脚本文件位于同一层级。  
2. 赋予脚本执行权限（首次需要）：  
   ```bash
   chmod +x start_ubuntu.sh
   ```  
3. 执行脚本：  
   ```bash
   ./start_ubuntu.sh
   ```  
   - 脚本会自动检查并安装缺失依赖，同步前后端依赖，并在 `logs/` 目录输出 `backend.log` 与 `frontend.log`。  
4. 通过 `Ctrl+C` 可停止脚本并终止前后端服务。
